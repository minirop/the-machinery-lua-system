function update()
end

-- properties must be inside inner tables to keep the order.
properties = {
	{ enabled = true },
	{ disabled = false },
	{ amplitude = 42 },
	{ tag = "this is a tag" },
	{ names = {"one", "three", "five"}  }
}
